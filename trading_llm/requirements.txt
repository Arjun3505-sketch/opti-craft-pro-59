# --- Core backend ---
flask==3.0.3
flask-cors==5.0.0
gunicorn==23.0.0

# --- Data + Trading ---
yfinance==0.2.43
pandas==2.2.3
numpy==2.1.2
requests==2.32.3
marshmallow==3.22.0

# --- Supabase ecosystem (compatible set) ---
# --- Supabase ecosystem (compatible set) ---
supabase==2.3.5
supafunc==0.3.2
postgrest==0.15.0   # ✅ compatible with supabase<0.16.0
storage3==0.7.4
gotrue==2.8.0
httpx==0.25.2       # ✅ works with the above, no proxy conflict


# --- AI + Utils ---
google-generativeai==0.8.3
python-dotenv==1.0.1
websockets==12.0   # ✅ keep under 13 to avoid Supabase realtime issues

# --- Visualization / UI ---
plotly==5.17.0
streamlit==1.36.0
lxml==4.9.3
beautifulsoup4==4.12.2
